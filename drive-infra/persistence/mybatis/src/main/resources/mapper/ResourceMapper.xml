<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tdrive.mybatis.persistence.mybatis.resource.mapper.ResourceMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="resourceSeq">

        INSERT INTO TB_RESOURCE_1
        (
            USER_SEQ,
            PARENT_FOLDER_SEQ,
            RESOURCE_TYPE,
            RESOURCE_NAME,
            PATH,
            EXT,
            MIME_TYPE,
            SIZE_BYTE,
            REGISTER_USER_SEQ
        )
        VALUES
        (
            #{userSeq},
            #{parentFolderSeq},
            #{resourceType},
            #{resourceName},
            "path",
            #{ext},
            #{mimeType},
            #{sizeByte},
            1
        )

    </insert>
    
    <insert id="insertTableNumber" useGeneratedKeys="true" keyProperty="entity.resourceSeq">

        INSERT INTO TB_RESOURCE_${tableNumber}
        (
            USER_SEQ,
            PARENT_FOLDER_SEQ,
            RESOURCE_TYPE,
            RESOURCE_NAME,
            PATH,
            EXT,
            MIME_TYPE,
            SIZE_BYTE,
            REGISTER_USER_SEQ
        )
        VALUES
        (
            #{entity.userSeq},
            #{entity.parentFolderSeq},
            #{entity.resourceType},
            #{entity.resourceName},
            "path",
            #{entity.ext},
            #{entity.mimeType},
            #{entity.sizeByte},
            1
        )

    </insert>


</mapper>